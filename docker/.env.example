# ══════════════════════════════════════════════════════════════════════════════
# OpenClaw Docker — Environment Variables
# ══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values.
# For Coolify: set these directly in the Coolify UI environment variables.
# ══════════════════════════════════════════════════════════════════════════════

# ── AI Provider (at least one required) ───────────────────────────────────────
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=
XAI_API_KEY=
GROQ_API_KEY=
MISTRAL_API_KEY=
CEREBRAS_API_KEY=
VENICE_API_KEY=
MOONSHOT_API_KEY=
KIMI_API_KEY=
MINIMAX_API_KEY=
ZAI_API_KEY=
AI_GATEWAY_API_KEY=
OPENCODE_API_KEY=
SYNTHETIC_API_KEY=
COPILOT_GITHUB_TOKEN=
XIAOMI_API_KEY=

# ── Amazon Bedrock (uses AWS credential chain) ───────────────────────────────
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# ── Ollama (local models, no API key needed) ─────────────────────────────────
# OLLAMA_BASE_URL=http://host.docker.internal:11434

# ── HTTP Basic Auth (recommended) ────────────────────────────────────────────
AUTH_PASSWORD=
AUTH_USERNAME=admin

# ── Gateway ──────────────────────────────────────────────────────────────────
# Required! Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=
OPENCLAW_GATEWAY_PORT=18789

# ── Model selection ─────────────────────────────────────────────────────────
# Override the default primary model. Format: provider/model-id
# OPENCLAW_PRIMARY_MODEL=

# ══════════════════════════════════════════════════════════════════════════════
# ── Cloudflare Tunnel / Reverse Proxy Fix ────────────────────────────────────
# ══════════════════════════════════════════════════════════════════════════════
#
# When running behind a Cloudflare Tunnel (or any reverse proxy that adds
# X-Forwarded-* headers), OpenClaw's gateway classifies the connection as
# "remote" and rejects the Bearer token.
#
# Setting this to "true" empties the proxy headers (X-Real-IP, X-Forwarded-For,
# X-Forwarded-Proto) and sets Host to localhost, so the gateway sees nginx
# as a loopback connection. Also auto-configures trustedProxies and
# dangerouslyDisableDeviceAuth in openclaw.json.
#
OPENCLAW_STRIP_PROXY_HEADERS=true

# Your public domain (for CORS/allowed origins in the control UI)
# Comma-separated for multiple origins.
# Example: https://claw.example.com,https://claw-internal.example.com
OPENCLAW_ALLOWED_ORIGINS=
# ══════════════════════════════════════════════════════════════════════════════

# ── Port ─────────────────────────────────────────────────────────────────────
PORT=8080

# ── OpenClaw Git ref (for building from source) ──────────────────────────────
# Pin to a specific branch, tag, or commit hash
OPENCLAW_GIT_REF=main

# ── Channels (optional) ──────────────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=
DISCORD_BOT_TOKEN=
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
# WHATSAPP_ENABLED=true

# ── Hooks (optional) ─────────────────────────────────────────────────────────
# HOOKS_ENABLED=true
# HOOKS_TOKEN=your-shared-secret
# HOOKS_PATH=/hooks

# ── Browser sidecar (optional) ───────────────────────────────────────────────
# BROWSER_CDP_URL=http://browser:9223
# BROWSER_EVALUATE_ENABLED=true
# BROWSER_DEFAULT_PROFILE=openclaw

# ── Extra system packages (optional) ──────────────────────────────────────────
# OPENCLAW_DOCKER_APT_PACKAGES=ffmpeg build-essential

# ── Custom init script (optional) ─────────────────────────────────────────────
# OPENCLAW_DOCKER_INIT_SCRIPT=/data/init.d/init.sh
